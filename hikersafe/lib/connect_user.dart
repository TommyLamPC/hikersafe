import 'dart:ffi';

import 'package:flutter/material.dart';

class ConnectUser extends StatelessWidget {
  const ConnectUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
